CREATE TABLE IF NOT EXISTS %s (
    Account_Name text,
    argument_list text,
    Checkpoint text,
    comment text,
    ctime timestamp,
    depend text,
    energy text,
    Error_Path text,
    etime timestamp,
    exec_host text,
    exec_vnode text,
    executable text,
    Exit_status int,
    forward_x11_port int,
    Hold_Types text,
    history_timestamp timestamp,
    interactive boolean,
    Job_Id text,
    Job_Name text,
    Job_Owner text,
    job_state text,
    jobdir text,
    Join_Path text,
    Keep_Files text,
    Mail_Points text,
    Mail_Users text,
    mtime timestamp,
    Output_Path text,
    Priority int,
    project text,
    qtime timestamp,
    queue text,
    Rerunable text,
    Resource_List text,
    resources_used text,
    run_count int,
    sandbox text,
    server text,
    session_id int,
    Stageout_status int,
    Shell_Path_List text,
    stime timestamp,
    substate int,
    Submit_arguments text,
    umask int,
    Variable_List text,
    pbs_version text,
    PRIMARY KEY ((Job_Id), stime, mtime))